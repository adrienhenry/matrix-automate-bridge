version: '3.8'

services:
  matrix-automate-bridge:
    build: .
    container_name: matrix-automate-bridge
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
